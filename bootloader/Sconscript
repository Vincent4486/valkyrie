# SPDX-License-Identifier: BSD-3-Clause
"""
Bootloader builder
"""

import os
import re

from SCons.Action import Action
from SCons.Environment import Environment
from SCons.Script import Flatten

Import('TargetEnvironment')
TargetEnvironment: Environment

# =============================================================================
# Environment Setup
# =============================================================================

Env = TargetEnvironment.Clone()
SourcePath = Dir('#bootloader').abspath
Arch = TargetEnvironment['BuildArch']
Boot = TargetEnvironment['BootType']

ArchitecturePath = os.path.join(SourcePath, f'{Arch}-{Boot}')
LinkerScript = os.path.join(ArchitecturePath, 'linker.ld')

Stage1Sources = []
Stage2Sources = []
Stage3Sources = []


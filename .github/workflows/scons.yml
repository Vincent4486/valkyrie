name: Build and Deploy Images to Server

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      TOOLCHAIN_DIR: os_toolchain
    steps:
      - uses: actions/checkout@v3

      - name: Cache toolchain
        uses: actions/cache@v3
        with:
          path: ${{ env.TOOLCHAIN_DIR }}
          key: ${{ runner.os }}-toolchain-${{ hashFiles('**/.config') }}
          restore-keys: |
            ${{ runner.os }}-toolchain-

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y python3 python3-pip scons python3-sh
          # use the helper script to install all required packages
          python3 scripts/base/dependencies.py --yes

      - name: Ensure cross-toolchain
        run: |
          # exit immediately if cached toolchain exists, build only if missing
          python3 scripts/base/toolchain.py ${TOOLCHAIN_DIR} --ensure

      - name: Build full project
        run: |
          # pass the same toolchain path so scons can locate it
          scons toolchain=${TOOLCHAIN_DIR}


